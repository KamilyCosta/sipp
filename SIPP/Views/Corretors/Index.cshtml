@model IEnumerable<SIPP.Models.Corretor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TemDeTrabalho)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CRECI)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TemDeTrabalho)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CRECI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CorretorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CorretorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CorretorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
